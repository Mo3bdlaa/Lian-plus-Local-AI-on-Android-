<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!--
        Model weights are gigabytes and re-downloadable; chat history stays on
        the device by choice. Nothing here is backed up to the cloud.
    -->
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="models/" />
    </device-transfer>
</data-extraction-rules>
